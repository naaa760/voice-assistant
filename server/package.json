{
  "name": "voice-assistant-server",
  "version": "1.0.0",
  "description": "Backend server for voice assistant application",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon --ignore 'client/*' server.js",
    "build": "echo 'No build step required for server'"
  },
  "dependencies": {
    "@deepgram/sdk": "^2.4.0",
    "axios": "^1.6.2",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "multer": "^1.4.5-lts.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
